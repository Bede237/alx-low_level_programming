#include "main.h"
#include <string.h>
/**
 * _strlen - gives the length of a string
 * @s: pointer to the string
 * Return: returns len
 */
int _strlen(char *s)
{
	int len = strlen(s);

	return (len);
}
